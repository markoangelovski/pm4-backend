import { ErrorResponseFilter } from './error-response.filter';

describe('ErrorResponseFilter', () => {
  it('should be defined', () => {
    expect(new ErrorResponseFilter()).toBeDefined();
  });
});
